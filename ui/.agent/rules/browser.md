---
trigger: always_on
---

Check the routes to see which routes require an :id before it.
For example, the storyboard route is :id/storyboard.

Test changes by browsing to http://localhost:4200/test-id/[route]
