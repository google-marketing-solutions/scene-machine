{
  "firestoreDatabase": "$FIRESTORE_DB",
  "gcpProject": "$PROJECT",
  "gcpLocation": "$REGION",
  "gcsBucket": "$GCS_BUCKET",
  "tasksQueuePrefix": "$TASKS_QUEUE_PREFIX",
  "backendApi": {
    "baseUrl": "/api",
    "apiKey": "$API_KEY"
  }
}
